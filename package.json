{
  "name": "drain-monitoring",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "sst dev --stage dev",
    "build": "sst build",
    "deploy": "sst deploy --stage staging",
    "remove": "sst remove --stage dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22",
    "typescript": "^5"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1009.0",
    "@aws-sdk/client-comprehend": "^3.1009.0",
    "@aws-sdk/client-dynamodb": "^3.1009.0",
    "@aws-sdk/client-ses": "^3.1009.0",
    "@aws-sdk/client-sesv2": "^3.1009.0",
    "@aws-sdk/lib-dynamodb": "^3.1009.0",
    "sst": "^3"
  }
}
